# ─────────────────────────────────────────────────────────────────────────────
# SECRETS — never commit real credentials
# ─────────────────────────────────────────────────────────────────────────────

# Your personal config with real credentials
config.env

# Active commit message — often in a half-written state, not useful to track
commit.msg

# Local handoff and instruction documents (real files, gitignored)
first_steps.md
project_instructions.md
project_knowledge.md

# Keep the example templates tracked
!project_instructions.md.example
!project_knowledge.md.example

# Log files
logs/*.log

# ─────────────────────────────────────────────────────────────────────────────
# RADICALE RUNTIME DATA
# ─────────────────────────────────────────────────────────────────────────────

# Hashed passwords file generated by setup.sh
config/users

# Your actual calendar and contact data
data/

# ─────────────────────────────────────────────────────────────────────────────
# CLOUDFLARE TUNNEL CREDENTIALS
# ─────────────────────────────────────────────────────────────────────────────

# Tunnel credential JSON — treat this like a private key
cloudflared-config/creds/

# ─────────────────────────────────────────────────────────────────────────────
# KEEP EMPTY DIRECTORIES
# .gitkeep files are used to preserve directory structure in the repo
# without committing any real content
# ─────────────────────────────────────────────────────────────────────────────
!cloudflared-config/creds/.gitkeep
!config/.gitkeep
!data/.gitkeep
